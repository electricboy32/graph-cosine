Did not correctly change a variable name when updating, it changed the name of the function but did not change the name when it is called.
I pointed this out and it still did not change it correctly. I manually changed it after this and the program ran as expected.